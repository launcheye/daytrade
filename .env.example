# daytrade environment overrides — copy to .env and edit.
# Every variable here is OPTIONAL. The platform runs fully offline with mock data.
#
# SAFETY: there is no variable that enables real trading. By design.
# Real order execution always raises NotImplementedError.

# --- Runtime ---
# Override the active config profile (configs/<profile>.yaml).
DAYTRADE_PROFILE=default

# Logging verbosity: DEBUG | INFO | WARNING | ERROR
DAYTRADE_LOG_LEVEL=INFO

# Force deterministic behaviour (fixed RNG seeds). Recommended for tests.
DAYTRADE_DETERMINISTIC=true

# --- Optional public market data ---
# When false (default), exchange clients return mock data and never hit the network.
DAYTRADE_ALLOW_NETWORK=false

# --- Optional macro AI (Gemini) ---
# When unset, the macro engine uses the deterministic mock analyzer.
# GEMINI_API_KEY=

# --- Optional exchange SANDBOX (testnet) keys ---
# SANDBOX = exchange TESTNET only. These keys must be TESTNET keys with
# trading scope DISABLED (read-only) and WITHDRAWAL scope DISABLED. The
# platform verifies key restrictions on connect and refuses any key that
# carries withdrawal permission. There is no mainnet execution path.
#
# Binance Spot Testnet: https://testnet.binance.vision/
# BINANCE_TESTNET_API_KEY=
# BINANCE_TESTNET_API_SECRET=
#
# Bybit Testnet: https://testnet.bybit.com/
# BYBIT_TESTNET_API_KEY=
# BYBIT_TESTNET_API_SECRET=
